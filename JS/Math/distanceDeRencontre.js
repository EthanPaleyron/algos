// Code is => INCOMPLETE❌
function distanceDeRencontre(d1, d2) {
  let result;
  const dTotal = d1 + d2;
  const v1 = dTotal / d1;
  const v2 = dTotal / d2;
  return result;
}

console.log(distanceDeRencontre(160, 180));
