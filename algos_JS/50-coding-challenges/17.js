// 🔴Calculate the sum of digits of a positive integer number
// Code is => INCOMPLETE❌

let positiveIntegrNumber = 325;

positiveIntegrNumber.toString();

for (let i = 0; i < positiveIntegrNumber.toString; i++) {
    positiveIntegrNumber += i;
}

console.log(positiveIntegrNumber);