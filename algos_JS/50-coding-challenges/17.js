// 🔴Calculate the sum of digits of a positive integer number
// Code is => INCOMPLETE❌

function positiveIntegrNumber(numbers) {
    let array = numbers.split("");
    for (let i = 0; i < array.length; i++) {
        array[i];
        array.push(array[i]);
    }
    let string = array.toString(array);
    return string;
}

console.log(positiveIntegrNumber(5715));